const Task1_Intro = () => (
  <div>
    <h2>Introduction to SSR & SSG</h2>
    <p><strong>SSR</strong>: Rendered on every request (dynamic).</p>
    <p><strong>SSG</strong>: Pre-rendered at build time (static).</p>
    <p>Both improve performance and SEO by avoiding client-only rendering.</p>
  </div>
);
export default Task1_Intro;
